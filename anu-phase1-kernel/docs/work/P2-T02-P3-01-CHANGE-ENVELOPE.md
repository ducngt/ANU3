# P2-T02 + P3-00/P3-01 Change Envelope

## Human-approved intent
Complete the technical work before the next Human G3: multimodal ingestion, University Memory/retrieval advancement, plus P3 contract-only Capability Registry/Smart Box discovery.

## Architecture delta
Add immutable artifact/object storage adapter, extraction pipeline, retrieval projection, Capability Registry and Smart Box Manifest Registry. Preserve P1/P2 accepted contracts.

## Contract delta
Adds multimodal ingest/result, integrity, knowledge materialization, retrieval query/result, capability contract, smart box manifest and discovery contracts.

## Authority/risk boundary
No new institutional authority is created. Real personal data remains out of scope. P3 execution runtime remains closed.

## Verification
Migration cycle, full tests, ARU-01 multimodal pilot, provenance replay, recovery, independent boundary verifier, PostgreSQL offline compile and live PostgreSQL CI gate.
